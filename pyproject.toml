[build-system]
requires = ["setuptools>=45,<81", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "picassosr"
dynamic = ["version"]
authors = [
    {name = "Joerg Schnitzbauer", email = "joschnitzbauer@gmail.com"},
    {name = "Maximilian T. Strauss", email = "straussmaximilian@gmail.com"},
    {name = "Rafal Kowalewski", email = "rafalkowalewski998@gmail.com"},
]
maintainers = [{name = "Rafal Kowalewski", email = "rafalkowalewski998@gmail.com"}]
keywords = ["super-resolution", "DNA-PAINT", "microscopy"]
description = "Collection of tools for painting super-resolution images"
readme = "readme.rst"
requires-python = ">=3.10,<3.15"
license = "MIT"
license-files = ["LICENSE.txt", "LICENSES/*.txt"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
    "PyQt6>=6.10.2,<7",  # prevent backward incompatible changes
    "numpy>=2.2.6,<3",
    "matplotlib>=3.10.7,<4",
    "h5py>=3.15.1,<4",
    "numba>=0.62.1,<1",
    "scipy>=1.15.3,<2",
    "pandas>=2.3.3,<3",
    "tables>=3.10.1,<4",
    "pyyaml>=6.0.3,<7",
    "scikit-learn>=1.5.0,<1.8",  # HDBSCAN with cluster_selection_epsilon > 0
    "tqdm>=4.67.1,<5",
    "streamlit>=1.50.0,<2",
    "nd2>=0.10.4,<1",
    "sqlalchemy>=2.0.44,<3",
    "plotly-express>=0.4.1,<1",
    "watchdog>=6.0.0,<7",
    "psutil>=7.1.1,<8",
    "playsound3>=3.2.8,<4",
    "imageio>=2.37.0,<3",
    "imageio-ffmpeg>=0.6.0,<1",
    "tifffile>=2024.7.24,<2027",
    "PyImarisWriter==0.7.0; sys_platform=='win32'",
    "hdf5plugin; sys_platform=='win32'"
]

[project.optional-dependencies]
cuda11 = [
    "numba-cuda[cu11]"
]
cuda12 = [
    "numba-cuda[cu12]"
]
cuda13 = [
    "numba-cuda[cu13]"
]
# GPU rendering in Render (WebGPU via wgpu: Metal, Direct3D 12 or
# Vulkan; any vendor). Pinned to a minor version: the shader and API
# surface are validated against it.
wgpu = [
    "wgpu>=0.32,<0.33"
]
# Convenience alias for all GPU features with the most common CUDA
# toolkit (12.x). For other toolkits install picassosr[cuda11,wgpu] or
# picassosr[cuda13,wgpu] instead.
gpu = [
    "picassosr[cuda12,wgpu]"
]
dev = [
    "build",
    "black",
    "pre-commit",
    "twine",
]
# Optional movie-format readers (czifile and liffile require Python >=
# 3.12). Used by picasso.io to open Zeiss .czi / Leica .lif files;
# absent installs simply do not advertise those formats.
czi = [
    "czifile>=2026.6.12; python_version>='3.12'",
    "imagecodecs>=2026.6.6; python_version>='3.12'",
]
lif = [
    "liffile>=2026.4.11; python_version>='3.12'",
    "imagecodecs>=2026.6.6; python_version>='3.12'",
]
formats = [
    "picassosr[czi,lif]",
]
# Not for general use: the installer / installer_gpu extras exist only to
# build the one-click installers, where every version is pinned exactly so
# the frozen bundles stay reproducible and stable. Regular users should
# install picassosr (optionally with [gpu] / [formats]) instead.
installer = [
    "PyQt6==6.11.0",
    "numpy==2.4.4",
    "matplotlib==3.10.8",
    "h5py==3.16.0",
    "numba==0.65.0",
    "scipy==1.17.1",
    "pandas==2.3.3",
    "tables==3.11.1",
    "pyyaml==6.0.3",
    "scikit-learn==1.7.2",
    "tqdm==4.67.3",
    "streamlit==1.56.0",
    "nd2==0.11.3",
    "sqlalchemy==2.0.49",
    "plotly-express==0.4.1",
    "watchdog==6.0.0",
    "psutil==7.2.2",
    "playsound3==3.3.1",
    "imageio==2.37.3",
    "imageio-ffmpeg==0.6.0",
    "tifffile==2026.4.11",
    "czifile==2026.6.12",
    "liffile==2026.4.11",
    "imagecodecs==2026.6.6",
    "wgpu==0.32.0",
    "pyinstaller==6.19.0",
    "PyImarisWriter==0.7.0; sys_platform=='win32'",
    "hdf5plugin==6.0.0; sys_platform=='win32'"
]
# GPU build: identical to the CPU installer plus the CUDA runtime.
# Reuse picassosr[installer] so the pinned versions never drift apart.
installer_gpu = [
    "picassosr[installer]",
    "numba-cuda[cu12]==0.30.2"
]

[project.urls]
Homepage = "https://github.com/jungmannlab/picasso"
Documentation = "https://picassosr.readthedocs.io/en/latest/index.html"
Changelog = "https://github.com/jungmannlab/picasso/blob/master/changelog.md"
Publications = "https://doi.org/10.1038/nprot.2017.024"

[project.scripts]
picasso = "picasso.__main__:main"

[tool.setuptools.dynamic]
version = {attr = "picasso.version.__version__"}

[tool.setuptools.packages.find]
include = ["picasso*"]

[tool.setuptools.package-data]
picasso = [
    "gui/icons/*.ico",
    "gui/icons/*.png",
    "config_template.yaml",
]

[tool.pytest.ini_options]
markers = [
    "slow: long-running tests (multiprocessing, Bayesian fits, compare_models)",
    "gui: tests that build Qt widgets (applied automatically to anything using the qapp / qt_offscreen fixtures); deselect with -m \"not gui\"",
    "gpu_backend: tests that exercise the real GPU render backend selection (every other test renders on the CPU reference, whatever the machine has)",
]

[tool.black]
line-length = 79
include = "\\.pyi?$"
exclude = '''
(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)
'''
